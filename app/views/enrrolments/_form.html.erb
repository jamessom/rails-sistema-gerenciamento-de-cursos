<%= form_for(@enrrolment) do |f| %>
  <% if @enrrolment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@enrrolment.errors.count, "error") %> prohibited this enrrolment from being saved:</h2>

      <ul>
      <% @enrrolment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date_enrrolment %><br>
    <%= f.datetime_select :date_enrrolment %>
  </div>
  <div class="field">
    <%= f.label :Student_id %><br>
    <%= f.text_field :Student_id %>
  </div>
  <div class="field">
    <%= f.label :Team_id %><br>
    <%= f.text_field :Team_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
